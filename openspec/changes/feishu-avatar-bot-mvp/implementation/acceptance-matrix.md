# Capability 验收场景矩阵

## 1. `feishu-collab-gateway`
1. 私聊首次消息创建会话（成功）
2. 群聊 @ 指令复用会话（成功）
3. 缺少 `project_id` 拒绝（失败）
4. 飞书回调重复投递去重（幂等）
5. 无权限联系人请求被拒绝（权限）

## 2. `tool-session-bridge`
1. 同协作会话绑定两个工具会话（成功）
2. use 切换活动会话成功（成功）
3. 切换到不存在会话返回 404（失败）
4. 重复执行 use 对同目标返回幂等结果（幂等）
5. 非参与者执行 peek 被拒绝（权限）

## 3. `hitl-interaction-loop`
1. NEED_USER_INPUT 触发创建 PENDING 请求（成功）
2. 用户提交 choice 回灌成功（成功）
3. run 不活跃回灌失败（失败）
4. 重复回调返回 NOOP_IDEMPOTENT（幂等）
5. 非审批人提交高风险确认被拒绝（权限）

## 4. `support-qa-and-policy-control`
1. 已收录 API 问题返回答案+来源（成功）
2. L1 命令自动执行并记录审计（成功）
3. 非白名单命令拒绝（失败）
4. 同一审批回调重复投递不重复执行（幂等）
5. L3 命令缺少双确认拒绝（权限）
